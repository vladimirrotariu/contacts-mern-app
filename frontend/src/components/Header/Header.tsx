import React from "react";
import "./Header.css";

const Header = (): React.ReactElement => {
  return (
    <header className="header">
      <h1>Contacts</h1>
    </header>
  );
};

export default Header;
